import { Routes } from "@angular/router";
import { authGuard } from "../core/guards/auth.guard";

// NOTE: We're keeping this file, but we're NOT using it for the main router configuration
// It should only be used for standalone component routing if needed in the future
export const routes: Routes = [];
